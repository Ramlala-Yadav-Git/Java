import javax.lang.model.util.ElementScanner14;
import javax.print.event.PrintEvent;

public class Operators {
    public static void main(String arr[]) {
        // operators are the symbol's those are used to perform actions
        System.out.println("hello ");

        /*
         * Arithmatic operators -> for mathmatical operations
         * * / + -
         * 
         * 
         * Relational operators-> for comparision
         * < > >= <= == !=
         * 
         * 
         * Logical operatrs -> && or
         * 
         * 
         * Ternary operator ->
         * expression ? value1 : value2
         * 
         * 
         * Assingment operators =>
         * = += -= /= *=
         * 
         * Bitwise operators -> works on bits
         * Bitwise AND &
         * Bitwise exclusive OR ^
         * Bitwise inclusive OR |
         * 
         * 
         * Unary operator -> ek hi operand pe operation perform hoga
         * post fix -> a++, prefix -> --a
         * 
         * 
         * 
         * 
         * 
         */

        int n = 0;
        if (n > 0) {
            System.out.println(n);
        } else if (n <= 0 && n > -2) {
            System.out.println("No n is not grater than 0 but greater than -2");
        } else {
            System.out.println("Non of them are true");

        }

        /*
         * switch(value)
         * {
         * case 0:
         * ....
         * break;
         * case 1:
         * ....
         * break;
         * default :
         * .....
         * }
         */

    }
}